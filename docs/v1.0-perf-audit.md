# v1.0 performance audit (12k library)

Working log for readiness checklist Step 2. **Do not** paste filenames, folder paths, titles, or screenshots of the wall. Record **counts, seconds, and feel** only.

- Date: 2026-09-03
- App: Skagway **0.80.0**
- Library: borrowed ~12k videos (content not inspected)
- Method: user stopwatch + subjective UI feel. No Instruments this pass.

## Verdict: PASS (watch: sort)

All A–M scenarios met the bar. Sort (L) is the slowest path (~2–2.5 s) — acceptable, not a fail. No signposts/Instruments unless we later chase sort.

## Library snapshot

| Fact | Value |
| --- | --- |
| Toolbar count (All Videos, no search/filter) | **12,174** |
| Grid or List at open | Grid |
| Thumbnails already cached? | Most cards have stills |
| Cold launch | Dock click → usable grid **3 s**; File → Open Recent → open library **4 s** |

## Pass bar (12k, already-indexed library)

| Scenario | Pass | Fail |
| --- | --- | --- |
| Cold start to usable grid | Window + first screen of cards within **~8 s**; app not beachballed | Spin / unresponsive **>15 s** |
| Filter / search apply | Result count updates within **~2 s**; window stays clickable | Multi-second freeze; beachball |
| Scroll / Home / End | Continuous motion; brief thumbnail pop-in OK | Stutter that stalls the scroll, or **>2 s** freeze |
| Sort (same membership) | Reorder without full-grid “flash rebuild” | Whole grid tears down / long hitch |
| Select 1, then 20 | Inspector updates; grid does not re-layout | Grid hitch or inspector lag **>1 s** |
| Play / Esc | Player opens/closes without reshaping or rebuilding the wall | Wall relayout, scroll jump, or long hitch |

Thumbnail **backfill** while scrolling (placeholders → stills) is expected and is not a fail by itself.

## Results

| ID | Scenario | Result | Seconds | Notes (no titles/paths) |
| --- | --- | --- | --- | --- |
| A | Cold start, Grid, All Videos | **pass** | 3 s dock; 4 s Open Recent | Usable grid |
| B | Scroll Grid ~2–3 screens, then fling | **pass** | — | Smooth |
| C | Home, then End | **pass** | &lt; 0.5 s each | |
| D | Quick Filter: one rating star (exact, Or Higher off) | **pass** | &lt; 0.25 s | |
| E | Clear filters | **pass** | &lt; 0.5 s | |
| F | Search: one common letter, wait for count | **pass** | ~1 s | Query `O` (letter only) |
| G | Clear search (×) | **pass** | &lt; 0.5 s | |
| H | Click one card; click another | **pass** | 0.25–&lt;1 s | |
| I | Shift-select a short range (~20) | **pass** | &lt; 0.5 s | |
| J | Space to play, Esc to stop | **pass** | &lt; 0.5 s | |
| K | Switch to List (⌘2); Home/End | **pass** | ⌘2 &lt;1 s; Home/End &lt;0.25 s | |
| L | Change Sort (Date Added / Title) | **pass** (watch) | Date Added ~2.5 s; Title ~2 s | Slowest scenario; no reported tear-down |
| M | Surprise Me (⇧⌘S) | **pass** | ~1 s | |

### Follow-ups

- **Sort (L)** is the only near-bar item. Revisit only if it grows or the grid visibly rebuilds.
- Scan for New Videos, re-encode, and Play All across 12k were **not** part of this pass.
