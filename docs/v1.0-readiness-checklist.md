## v1.0 Readiness Checklist

This checklist turns `ROADMAP.md`’s **Path to v1.0** into an evidence-backed, trackable pass.

### Current context
- App version: `v0.80.0` (from `project.yml`)
- `ROADMAP.md` Current State is **as of v0.80.0**. Feature audit (§3) is complete.

---

## 1) Regression test suite (current unit tests)

### Status: COMPLETE (2026-09-03) — expansion landed
- [x] Confirm what `SkagwayTests` currently covers (run tests, capture results)
  - First run: **74 tests**, 0 failures
  - After expansion: `xcodebuild test -project Skagway.xcodeproj -scheme Skagway -destination 'platform=macOS'`
  - Result: **TEST SUCCEEDED**
  - Summary: **107 tests executed, 0 failures**
- [x] Expand coverage for filtering / collections / migrations / duplicates
- [x] Filtering & Quick Filter — `RatingQuickFilterTests` (exact, No Stars, Or Higher 1–4, ignored at 0 and 5)
- [x] Collections AND/OR — `FilterMatcherTests` (empty group, ALL, ANY, two-level OR-of-AND, title/name, unknown custom field)
- [x] Album order — existing `AlbumPlaylistOrderTests`; Play All advance — `PlayAllAdvanceTests` (skip missing, end vs loop)
- [x] Corrupt heuristic — `VideoIntegrityTests` (metadata vs thumbnail-settled). Full `refreshMissingCount` FS walk is still UI-triggered; missing files are covered via Play All skip
- [x] Migrations — `DatabaseMigrationTests` (empty DB has latest tables/columns including `sortIndex`; migrate twice is idempotent)
- [x] Duplicates fingerprint — `ContentFingerprintTests` (identical, head/tail/size differ, missing file, stability)
- [x] Deterministic — temp files only; no ffmpeg

---

## 2) Performance audit (10k+ videos)

### Status: COMPLETE (2026-09-03) — **PASS** (watch: sort)
12,174 videos, Grid, most thumbnails cached. Full table: [`docs/v1.0-perf-audit.md`](v1.0-perf-audit.md).

- [x] Define target scenarios (A–M)
- [x] Pass/fail bar written
- [x] User filled snapshot + A–M
- [x] Filter / search: D &lt;0.25 s, F ~1 s, E/G &lt;0.5 s
- [x] Scroll / thumbnails: B smooth; C &lt;0.5 s
- [x] Selection / inspector: H 0.25–1 s, I &lt;0.5 s
- [x] Playback open/close: J &lt;0.5 s
- [x] List + Surprise Me: K ⌘2 &lt;1 s; M ~1 s
- Watch: **L sort** Date Added ~2.5 s, Title ~2 s — pass, slowest path

Cold start: 3 s from Dock, 4 s from Open Recent. No Instruments this pass.

---

## 3) Feature audit against the roadmap (ship vs half-finished)

### Status: COMPLETE (2026-09-03)
Method: `CHANGELOG.md` through **0.80.0 (build 1011)** + targeted source checks (no new 10k library run; that is Step 2). `ROADMAP.md` Current State rewritten to **as of v0.80.0**.

Verdict: **Nothing in the v1.0 candidate set is half-finished.** Phase 2/3 items below stay out of 1.0. Remaining 1.0 risk is verification (tests, 10k perf, review, security), not missing product surface.

### Roadmap “Current State” (v0.28 wording) → v0.80.0

| Item | Status | Evidence |
| --- | --- | --- |
| Curated Wall + Quick/Advanced Filter + Quality chips + filter↔collection | **Shipped; needs verification** | Drawer, pills, `applyFilters`; no unit tests for filter/collection truth table |
| Unified playback (inline → Compact/Windowed/Full; Play from Beginning; sidecar SRT; traffic lights; resume on cards) | **Shipped; needs verification** | `InlinePlaybackController`; 0.77–0.78. Play All / Loop added later (see new features) |
| Collections two-level AND/OR | **Shipped; needs verification** | `CollectionRuleGroup`; no dedicated collection-eval tests |
| Duplicates + ContentFingerprint + “Not a Duplicate” | **Shipped; needs verification** | `ContentFingerprint.swift`; grid/list; no fingerprint stability tests |
| Re-encode / Move queues (crash-safe, abort, pills) | **Shipped; needs verification** | `VideoConversionQueue`, `VideoMoveQueue`; error-path review is Step 4 |
| Tags + custom metadata (per-library, sort/list columns) | **Shipped; needs verification** | 0.75–0.76; inspector + list columns |
| Thumbnails + filmstrips + Set Poster from Image | **Shipped; needs verification** | `ThumbnailService`; poster from image 0.77.4; cache now per-library (0.73) |
| Bookmarks (⌥⌘B) | **Shipped; needs verification** | 0.76.2 |
| Keyboard: Home/End, grid arrows, shortcut rationalization | **Shipped; needs verification** | 0.77.5–0.77.6 |
| Missing (manual refresh) + Corrupt (recheck on select) | **Shipped; needs verification** | `refreshMissingCount`, `refreshMetadataIfCorrupt`; no FS-level tests |
| DnD import, empty-library invite, exclude folders, Last Added | **Shipped; needs verification** | Exclude folders **0.78.0** (was listed as Phase 2 — **done**) |
| Library titles, Bulk Rename, library home/cache setup | **Shipped; needs verification** | BulkRename tests exist; library home 0.73 |
| Sparkle in-app | **Shipped; publish is GTM not 1.0** | Updater + appcast emit; hosting/feed publish still GTM |
| Help URL + activity strip | **Shipped and verified** (docs/UI) | 0.78.3–0.78.4 |

### Shipped after v0.28 (must be in Current State; 1.0-relevant)

| Item | Status | Notes |
| --- | --- | --- |
| Albums + playlist `sortIndex` + grid/list DnD reorder | **Shipped; needs verification** | AlbumPlaylistOrder tests exist; playback-advance edge cases still listed in Step 1 |
| Play All (⌘⇧P) = **current filtered view**; Loop **only** during Play All session | **Shipped; needs verification** | 0.79.x; stopping / single play ends session |
| Rating Quick Filter: No Stars, Or Higher, disabled at 0 and 5 | **Shipped; needs verification** | 0.80.0; session-only `ratingFilterOrHigher`; no filter unit tests |
| Search: title, file name, original file name, tags, custom fields | **Shipped; needs verification** | 0.75.0 — Phase 2 “search beyond FTS5” is largely **done** |
| Per-library thumbnail/filmstrip cache | **Shipped; needs verification** | 0.73; Save Copy keeps original cache pointer |
| Import play counts + resume; Original File Name; four-value subtitles | **Shipped; needs verification** | 0.76.0 |
| Inspector width / filters drawer height persistence | **Shipped and verified** (prefs + code) | `filtersDrawerHeight`, inspector prefs |

### Explicitly not required for v1.0

| Item | Status |
| --- | --- |
| Auto-import / watch folders | **Not shipped** — Phase 2; no `FSEvents` / watch-folder implementation |
| Built-in notes field | **Won’t add** — a custom **Text** field covers this; see clarifications |
| Semantic / AI search | **Not shipped** — Phase 3 |
| User manual | **Lives in machii-labs** (`/skagway/manual`). Repo `docs/USER_GUIDE.md` is a stub pointer. Incomplete vs 0.80 — Step 6 |
| Mac App Store | **Dropped** — never planned; stay Developer ID + unsandboxed |
| `os_signpost` | **Not in source** — optional timing for Step 2, not a product feature (see below) |

### Half-finished?

**None** in the 1.0 product set. Closest “partial” items are **process** (Sparkle feed not published, manual lagging 0.80, test gaps), not incomplete features.

### Clarifications (2026-09-03)

**Notes vs custom field.** There is no strong need for a built-in Notes column. Custom metadata already has type **Text** (multiline). Users can add a field named Notes (or anything else); it appears in the Inspector, Sort, List columns, Advanced Filter / collections, search, and export. A first-class field would only be justified if every library must have Notes without setup, or if we needed a special UI (always-on Inspector block, dedicated shortcut). That is not a 1.0 requirement.

**`os_signpost` (what it is).** Apple’s interval markers in Unified Logging (`import os` / `OSSignposter`). You wrap a hot path (`beginInterval` / `endInterval`); Instruments’ Points of Interest and `log show --predicate 'subsystem == "…"'` show how long that interval took. `GRID-PERFORMANCE.md` proposed signposts around `applyFilteredVideos`, grid `scrollTo`, thumbnail generate, and `contentHost.rootView =`. **None of that was implemented** (or it was removed). It is a **developer profiling aid**, not user-facing, not required to ship 1.0. Step 2 can use Instruments Time Profiler instead; add signposts only if we want repeatable named timings in the log.

---

## 4) Code review (full-branch readiness pass)

### Status: COMPLETE (2026-09-03) — Bugbot found no bugs
There are **no outstanding feature branches**. The 1.0 candidate is `main`. The review used `Diff: natural language` over the focus paths (merge-base vs `main` would have been empty).
- [x] Bugbot pass via `skagway-code-review` (filters, Play All/Loop, albums, `filteredVideos`, playback, queues)
- [x] No second branch-diff pass planned — nothing to merge
- [x] Feature audit already found no half-finished / placeholder 1.0 flows

---

## 5) Security audit (local-only, unsandboxed file & process safety)

### Status: COMPLETE (2026-09-03) — **PASS**
Note: [`docs/v1.0-security-audit.md`](v1.0-security-audit.md). Cursor Security Review found no issues in the working-tree delta; queues/ffmpeg/privacy were reviewed in shipped code (they were not in that delta).

- [x] Filesystem handling:
  - [x] Path compares use `standardizingPath` where libraries/renames matter; exclude-folder tests cover prefix mistakes
  - [x] Re-encode and move promote only after success / size verify; source delete last
  - [x] No shell interpolation of library/media paths
- [x] External process invocations:
  - [x] ffmpeg / ffprobe / launchctl use argv arrays
  - [x] ffmpeg path: executable check + fixed candidates; user binary is an accepted local trust
- [x] Privacy:
  - [x] No app `URLSession`; no analytics. Sparkle auto-check **off**. Help URL is user-initiated.

---

## 6) Documentation & production-readiness package

### Status: MANUAL CATCH-UP COMPLETE (2026-09-03); packaging already proven on 0.80.0
Copy lives in **machii-labs** `src/app/skagway/manual/manual.ts` (not this repo). Screenshots were not recaptured — some stills may predate Or Higher / album grips.
- [x] Manual source of truth: [machiilabs.com/skagway/manual](https://machiilabs.com/skagway/manual)
- [x] Search: title, file name, original file name, tags, custom fields
- [x] Rating: No Stars, exact, Or Higher (disabled at 0 and 5)
- [x] Albums: saved order, DnD, Play All (⌘⇧P), Loop session-only
- [x] Exclude folders (Data Sources)
- [x] Bulk Rename, Set Poster from Image, Boolean custom fields, per-library cache
- [x] Packaging: Developer ID + notarized DMG last proven on **0.80.0 (1011)** (`dist/Skagway-0.80.0-1011.dmg`)
- [ ] Sparkle appcast **publish** to downloads.machiilabs.com — GTM, not a manual gap (`docs/SPARKLE.md`)
- [x] Mac App Store / sandbox — out of scope

---

## Evidence capture

As we run checks, append short results here:
- Unit test run output: **TEST SUCCEEDED** (107 tests, 0 failures) after Step 1 expansion
  - XCRESULT: `/Users/pkleim/Library/Developer/Xcode/DerivedData/Skagway-dfcsciayuogrtgbmeywiilffljgq/Logs/Test/Test-Skagway-2026.09.03_09-02-23--0700.xcresult`
- Performance audit: **PASS** on 12,174 videos (2026-09-03) — [`docs/v1.0-perf-audit.md`](v1.0-perf-audit.md); watch sort ~2–2.5 s
- Feature audit notes vs roadmap: **complete 2026-09-03** — see §3; `ROADMAP.md` Current State updated to v0.80.0
- Code review: **COMPLETE** — Bugbot found no bugs (2026-09-03). No outstanding feature branches; candidate is `main`.
- Security audit: **PASS** (2026-09-03) — [`docs/v1.0-security-audit.md`](v1.0-security-audit.md)
- Manual: **caught up to 0.80** in machii-labs `src/app/skagway/manual/manual.ts` (2026-09-03); screenshots not recaptured; Sparkle feed publish remains GTM

